/**
 * Operator visual-acceptance markers — presentation overlay only.
 * Never used by production customer invoice issuance or downloads.
 */

/** Banner / PDF overlay when `generateSalesInvoicePdf(..., { preview: true })`. */
export const OPERATOR_TEST_DOCUMENT_INDICATOR = "TEST DOCUMENT — NOT AN INVOICE";
